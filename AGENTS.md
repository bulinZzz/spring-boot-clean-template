# AI 编码约束规范

> 本文件定义 AI Agent 在本仓库中的工作方式、架构边界与工程约束。
>
> AI Agent 在理解需求、设计方案、生成代码或重构前，必须遵守以下规则。
>
> 本文件承载项目级的 AI 工作规则与基础架构约束；具体架构事实以项目架构文档为准。

## 1. 工作方式

### 1.1 任务范围

开始任务时，先确定需求涉及的模块、层次与行为范围，并检查当前工作区状态。

不得覆盖、回退或清理用户已有修改。

读取代码应按需增量进行：

1. 理解模块对外能力时，先阅读目标模块的 `api/`。
2. 修改模块内部时，再根据相关架构规则定位实现与测试。
3. 不为理解局部任务而无差别读取整个仓库。

实现方案按以下优先级确定：

1. 优先复用项目中已有的语义匹配实现，保持局部一致。
2. 现有实现与架构约束冲突时，以架构约束为准。
3. 现有实现无法覆盖时，根据本文档与项目架构文档进行判断。
4. 无法合理判断时，选择最小变更、最小耦合且边界清晰的方案。

### 1.2 事实、规则与文档

各类信息只在其职责范围内作为依据：

```text
AI 工作规则与项目级约束 → AGENTS.md
架构事实与结构说明     → 项目架构文档
当前运行行为           → 代码与测试
局部设计意图           → 代码 / Javadoc
```

代码不得反向定义架构规则。

项目文档可以补充具体架构事实，但不得通过项目局部说明绕过本文件中的项目级约束。

当规范、文档、实现和测试存在不一致时，应识别冲突并保持修改范围可控，不得通过猜测掩盖冲突。

### 1.3 变更原则

只修改完成任务所必需的内容。

不得为了统一风格进行无关重构、批量迁移、目录重排或依赖替换。

新增抽象、共享组件或防腐层时，必须能够指出它当前解决的具体问题与真实使用方。

无法说明实际问题或使用方只存在于预期中时，不新增抽象。

## 2. 架构边界

架构采用整洁架构（Clean Architecture）、六边形架构（Hexagonal Architecture）与依赖倒置原则（DIP）。

本节与 §3、§4 的边界属于强制约束，适用于新增与修改的代码；既有代码不因此被要求整体改造。

依赖方向必须保持向内收敛：

```text
Web / Infrastructure → Application → Domain
```

统一判据是**依赖封闭**：

> 内层不依赖外层；领域不依赖技术框架；模块不依赖其他模块的实现细节。

### 2.1 Domain

`domain/` 只承载领域模型、领域规则及其必要抽象，例如 Repository 接口。

Domain 不依赖 ORM、依赖注入、事务、Web、序列化等框架能力。

允许使用不引入运行时框架依赖的编译期注解。

### 2.2 领域行为

聚合和实体负责维护自身状态与业务不变量。

约束聚合不变量的校验与状态变更必须在模型内部完成，外部只能通过领域行为修改聚合，不得绕过模型直接写入状态。

跨多个聚合、且无法合理归属单一领域模型的纯业务规则使用 Domain Service。

需要数据库、远程服务、当前用户、系统时间等外部事实的逻辑，不直接放入聚合模型。

Application Service 负责用例编排，不承载聚合内部业务规则。

Controller 负责 HTTP 协议适配、输入校验与契约转换。

### 2.3 输入校验

边界层负责输入自身的协议约束，包括必填性、类型、格式、长度与基本范围。

业务对象是否存在、当前状态是否允许操作，由 Application 判断。

领域不变量是否满足，由 Domain 判断。

不同层负责不同类型的有效性判断，不通过低层重复承担高层职责来掩盖边界设计。

## 3. 模块公开面与模型隔离

### 3.1 模块公开面

`module/<name>/api/` 是模块唯一公开面。

跨模块只能依赖对方 `api/` 中明确公开的契约。

禁止跨模块直接依赖：

* Domain Model
* Repository
* Application Service 实现
* Mapper
* PO
* Converter
* Infrastructure 实现

模块内部实现对其他模块不可见。

数据表归属单一模块，禁止跨模块直接访问其他模块的数据表。

### 3.2 API 契约

`api/` 中主要承载：

* 模块公开服务接口
* Command
* Query
* Result

Request / Response / VO 只属于 HTTP 边界。

Command / Query / Result 只属于模块公开契约。

二者不得混用。

公开契约应保持稳定。

删除或修改字段、类型、必填性、枚举语义以及方法参数或返回值语义等，均按契约变更处理，并检查调用方影响。

### 3.3 公开值类型

公开契约可以使用稳定、能够独立理解的值语义类型。

不应将聚合、实体或包含内部业务行为的 Domain 对象直接公开为模块间契约。

判断标准不是“它是不是一个值对象”，而是：

> 调用方是否可以在不依赖模块内部实现的情况下理解并正确使用它。

### 3.4 模型与组件

| 场景        | 职责 / 组件                  | 触发条件                    |
| --------- | ------------------------ | ----------------------- |
| HTTP 输入   | Request                  | 有 HTTP 输入               |
| HTTP 输出   | Response / VO            | 有 HTTP 输出               |
| HTTP 模型转换 | Assembler                | HTTP 模型需要与模块契约转换时       |
| 模块操作      | Command                  | 有公开操作                   |
| 模块查询      | Query                    | 有公开查询                   |
| 模块输出      | Result                   | 公开能力需要输出                |
| 领域业务模型    | Domain Model / Aggregate | 有业务状态或规则                |
| 跨聚合规则     | Domain Service           | 无法归属单一 Model            |
| 持久化抽象     | Repository               | 需要持久化                   |
| 用例实现      | Application Service      | 有用例编排                   |
| 出站能力抽象    | Port                     | 需要隔离外部能力                |
| 数据库模型     | PO                       | 有持久化模型                  |
| 持久化转换     | Converter                | 有 PO 与 Domain Model 的转换 |
| 外部系统适配    | Adapter                  | 有外部系统                   |
| 通用工具      | Shared Utility           | 有真实跨模块复用                |

具体目录划分遵循项目架构文档，不因本表机械引入不存在的组件。

### 3.5 模型隔离与转换

不同边界的模型不得为了便利而直接共享内部实现。

存在明确语义边界时，应在边界处完成显式转换，避免将某一层的内部模型带入另一层。

典型关系：

```text
HTTP Model ↔ API Contract
API Contract ↔ Domain Model
PO ↔ Domain Model
```

通常情况下：

* Web 模型与模块契约的转换由 Web Assembler 负责。
* 契约模型与 Domain Model 的转换由 Application 负责。
* PO 与 Domain Model 的转换由 Persistence Converter 负责。

简单转换可以内聚在所属组件。

只有当转换形成独立复杂逻辑或出现明确复用需求时，才提取专门组件。

**不要为了形式上的模型隔离，机械制造没有实际价值的重复类型与转换。**

### 3.6 Shared

`shared` 不承载具体业务领域。

只有真正跨模块共享、具有稳定语义且能够独立于具体业务模块存在的能力，才进入 `shared`。

不能因为多个模块当前恰好存在相似代码，就机械提取为共享组件。

## 4. Repository、Port 与外部依赖

指向外部技术的依赖必须经项目自己的边界进入内层。

边界抽象由需要该能力的内层声明，实现位于 Infrastructure。

### 4.1 Repository

Repository 表达领域持久化能力。

接口位于 Domain，并由 Infrastructure 提供实现。

Domain 只依赖 Repository 抽象，不感知具体持久化技术。

持久化实现负责完成 PO 与 Domain Model 的转换。

### 4.2 Application Port

Port 表达 Application 所需要的非持久化外部能力。

外部服务、消息、支付、文件存储等技术调用由 Adapter 封装。

Vendor 类型、协议模型与技术细节不得进入 Application 或 Domain。

### 4.3 第三方依赖与防腐

第三方依赖是否需要防腐，应根据其是否进入稳定业务语义、公开契约，或形成明显替换与治理成本判断。

需要防腐时，在合适边界建立项目自己的抽象。

第三方对象、协议模型与技术语义不得进入 Domain 或模块公开契约。

对于已经建立防腐层的能力，生产代码不得绕过防腐层直接依赖对应第三方实现。

防腐层缺少能力时，优先扩展现有边界，而不是让调用方绕过边界自行实现。

## 5. 工程规则

### 5.1 依赖注入

使用构造器注入。

禁止字段注入。

### 5.2 null

null 处理以信任边界为界：

* 边界输入在校验前视为不可信，由边界负责处理非法 null。
* 进入内部后，对预期非空的值保持严格契约，必要时快速失败。
* 正常缺失状态使用 `Optional` 等显式语义表达。
* 不得通过 null 容忍调用掩盖内部契约错误。

### 5.3 异常

异常只用于表达异常路径，不用于正常控制流。

* 没有差异化处理需求时，优先使用标准 JDK / 框架异常。
* 需要表达稳定业务语义时，使用项目统一的业务异常类型。
* 业务异常只表达业务语义，不携带 HTTP 状态码、HTTP 响应类型或其他外部协议语义。
* 只有当现有异常类型无法满足明确的差异化处理需求时，才新增异常类型。
* 差异化处理包括不同的捕获、日志、恢复、重试、补偿或外部协议映射行为。
* 业务代码通常不应捕获异常后仅为了重新包装或重新映射而再次抛出；第三方异常翻译为项目语义属于边界适配的例外。
* 异常应尽可能自然传播到能够正确处理它的边界。
* Web 边界统一将业务异常翻译为对应的 HTTP 错误响应。
* 未预期异常由 Web 边界统一兜底，完整异常信息进入日志，响应不得暴露内部实现细节。

### 5.4 事务

默认以一个完整的 Application Service 用例作为本地事务边界。

Domain 不声明事务。

不得通过扩大本地事务边界解决跨系统一致性问题。

本地事务只覆盖本地持久化操作；远程调用、消息发送、重试与补偿等跨系统行为不得被假定为本地事务的一部分。

## 6. 测试与验证

### 6.1 验证原则

每次代码变更都必须执行与变更风险相匹配的验证。

存在相关测试时，应优先运行受影响测试，并在必要时扩大验证范围。

验证的目的，是证明行为、边界与架构约束仍然成立，而不是机械追求测试数量或覆盖率。

### 6.2 修改前验证

任务开始后，应了解相关测试和构建的当前状态。

若受影响的测试或验证在修改前已经失败，应区分已有失败与本次变更引入的问题，不得将基线问题归因于本次修改。

### 6.3 修改后验证

根据变更范围选择验证方式，并优先扩展现有、职责匹配的测试类。

只有现有测试没有合适归属，或新增验证形成独立测试责任时，才创建新的测试类。

生产类与测试类不要求一一对应。

验证方向包括：

* Domain Model / Domain Service → 领域单元测试
* Application Service / 用例编排 → 用例测试
* Web 请求、响应或异常映射 → Web 层测试
* Persistence 查询、映射或数据库行为 → 持久化 / 集成测试
* 跨模块 API → 契约与调用行为测试
* 架构、包结构或依赖边界变化 → 架构约束测试

能够自动验证的内容应优先自动验证。

### 6.4 运行时冒烟验证

测试通过不等于应用可以按真实方式运行。

测试与真实启动在 classpath 组装、配置加载与资源加载上并不等价。

存在下列变更时，除自动化测试外，应做一次运行时冒烟验证：

* 修改资源文件、配置或数据库初始化
* 修改启动流程、Web 层配置或装配
* 修改外部系统集成
* 修改依赖
* 发布前验证

验证方式为：

> 启动应用并确认启动成功，执行一次真实调用，确认结果符合预期。

其他变更仍按影响范围选择验证方式，不要求每次修改都启动完整应用。

### 6.5 任务收尾审计

编码完成不是任务完成的标志。

收尾时应审计本次变更对架构边界、工程规则、编码约定与文档规则的遵循情况，并确认必要验证已经完成。

至少检查：

* diff 是否仅包含任务相关修改
* 模块 `api/` 是否仍为唯一公开面
* Domain 是否保持框架无关
* PO、Domain Model 与 Web / 契约模型是否正确隔离
* 是否新增无必要的共享抽象或技术耦合
* 是否遗漏受影响的测试、契约与转换
* 是否留下临时代码、无效 import 或未使用实现

发现问题时按行为风险处理：

* 表达级与结构级问题 → 修复后重新验证
* 行为级问题 → 先补测试明确预期行为或复现缺陷，再修复实现，由测试验证结果

交付时给出一行审计结论：

> 无发现；或说明发现的问题、修复结果与验证结果。

只报告能够说明“不修改会发生什么”的有效发现。

零发现同样是合格的审计结论。

无法执行必要验证时，必须明确说明未验证项及原因，不得将未验证结果表述为通过。

## 7. 编码约定

> 本章只定义稳定且会影响 AI 代码生成结果的默认编码约定。

### 7.1 Domain 封装

聚合根与领域实体按需提供访问能力。

不得生成无差别 setter 以暴露内部状态修改。

### 7.2 强类型 ID

具有稳定业务语义的对象标识，优先使用本模块自己的 `record` 值类型。

纯技术用途的 ID 不必为了形式统一而强行包装。

是否包装，应以是否能够改善语义表达与边界隔离为依据，而不是机械追求统一。

### 7.3 注释与 Javadoc

项目注释和 Javadoc 使用中文。

只记录代码本身无法充分表达、且对理解当前代码具有稳定价值的信息。

优先用于说明：

* 类的职责边界或稳定设计意图
* 调用者无法仅从签名和实现直接得出的契约、前置条件或业务规则
* 非显然的实现原因
* 外部依赖、配置或框架行为的必要说明

不要使用注释记录：

* 修改过程
* 历史方案
* 被否决的方案
* 阶段性决策
* 临时状态
* 可以直接从代码读出的实现步骤

新增注释前先判断：

> 删除这段注释后，读者是否会丢失代码本身无法提供的重要信息？

如果不会，则不写。

实现发生变化后，受影响的注释和 Javadoc 必须同步保持准确。

## 8. 文档

文档只描述最终状态和当前适用的设计。

不在当前文档中记录：

* 历史方案
* 已删除的实现
* 修改过程
* 尝试失败的方案
* 临时决策过程

历史信息由 Git 保留。

每类知识保持单一权威来源，其他位置只作必要的引用或局部说明：

```text
Agent 工作规则   → AGENTS.md
架构事实         → 项目架构文档
项目使用说明     → README.md
示例与参考       → 项目文档目录
局部设计意图     → 代码 / Javadoc
行为约束         → 测试
```

文档的名称与组织方式由项目决定（如架构文档可用 `ARCHITECTURE.md`，文档目录可用 `docs/`）；本表约定的是每类知识的唯一归属。

不要为了“文档完整”在多个位置复制同一规则。

## 9. 设计判据

面对未被具体条款覆盖的设计问题：

1. 优先使用已有且语义匹配的抽象，而不是新建。
2. 优先保持现有模块边界与依赖方向。
3. 不为了形式上的架构完整制造无实际价值的类型、转换层或抽象。
4. 不让技术实现反向侵入更内层。
5. 无法合理判断时，以最小变更、最小耦合和清晰边界为原则。

能够由现有架构判据直接得到的结论，不额外制造规则。
